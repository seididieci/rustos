[package]
name = "userconsole"
version = "0.1.0"
edition = "2024"

# Crate fuori dal workspace del kernel: build freestanding separata.
[workspace]

[dependencies]
libr = { path = "../../libs/libr" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "z"
