[package]
name = "usertestcli"
version = "0.1.0"
edition = "2024"

[workspace]

[dependencies]
libr = { path = "../../libs/libr" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "z"
